FROM oraclelinux:9

RUN dnf makecache && dnf install mysql rsync zstd tar -y && dnf install -y https://dl.minio.org.cn/client/mc/release/linux-amd64/mc.rpm
WORKDIR /sqlCredential
COPY ./entrypointDumper.sh /entrypoint.sh
#ENTRYPOINT ["sh","-c","/entrypoint"]
ENTRYPOINT ["bash","-x","/entrypoint.sh"]
